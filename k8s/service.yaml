apiVersion: v1
kind: Service
metadata:
  name: oja-local-service
  namespace: default
  labels:
    app: oja-local
spec:
  selector:
    app: oja-local
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP